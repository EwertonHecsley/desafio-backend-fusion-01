generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Planet {
  id         String @id @default(uuid()) @db.Uuid
  name       String
  climate    String
  terrain    String
  population Int

  @@map("planets")
}
